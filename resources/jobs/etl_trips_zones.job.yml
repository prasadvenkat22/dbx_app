resources:
  jobs:
    etl_trips_zones_job:
      name: etl_trips_zones_job

      tasks:
        - task_key: run_trips_zones_pipeline
          pipeline_task:
            pipeline_id: ${resources.pipelines.trips_zones_pipeline.id}
